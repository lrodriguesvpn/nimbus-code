name: Adicionar issues/PRs à board do repositório

# Garante que toda issue/PR nova deste repositório seja adicionada
# automaticamente à board do PROJETO deste mesmo repositório.
#
# Autenticação:
#   - Preferencial: GitHub App (`NIMBUS_APP_ID` + `NIMBUS_APP_PRIVATE_KEY`)
#   - Fallback temporário: `VPNDEV_PROJECT_TOKEN`
#
# Se nenhuma credencial estiver disponível, este workflow registra warning e pula
# a automação para manter a compatibilidade com ambientes ainda não preparados.

on:
  issues:
    types:
      - opened
  pull_request:
    types:
      - opened

permissions:
  contents: read

jobs:
  add-to-repo-project:
    name: Adicionar à board do repositório
    runs-on: ubuntu-latest
    steps:
      - name: Generate GitHub App installation token
        id: app-token
        if: ${{ env.NIMBUS_APP_CONFIGURED == 'true' }}
        env:
          # 'secrets' não é permitido em jobs.<job_id>.steps.if (ver
          # https://docs.github.com/actions/reference/workflows-and-actions/contexts#context-availability) —
          # avaliar aqui em env (permitido) e ler via env.* no if acima.
          NIMBUS_APP_CONFIGURED: ${{ secrets.NIMBUS_APP_ID != '' && secrets.NIMBUS_APP_PRIVATE_KEY != '' }}
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.NIMBUS_APP_ID }}
          private-key: ${{ secrets.NIMBUS_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          github-api-url: ${{ github.api_url }}
          permission-metadata: read
          permission-issues: read
          permission-pull-requests: read
          permission-repository-projects: write
          permission-organization-projects: write

      - name: Resolve auth token (App or explicit migration fallback)
        id: resolve-token
        run: |
          if [ -n "${{ steps.app-token.outputs.token }}" ]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
            echo "token=${{ steps.app-token.outputs.token }}" >> "$GITHUB_OUTPUT"
            echo "::notice::Autenticado via GitHub App (instalação de curta duração)."
          elif [ "${{ vars.NIMBUS_AUTH_MIGRATION_MODE }}" = "pat" ]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
            echo "token=${{ secrets.VPNDEV_PROJECT_TOKEN }}" >> "$GITHUB_OUTPUT"
            echo "::warning::Modo de migração PAT explicitamente habilitado; owner, prazo, ambiente e auditoria são obrigatórios."
          else
            echo "::error::GitHub App não configurado; etapa cross-repo bloqueada."
            exit 1
          fi

      - name: Descobrir URL da board deste repositório
        if: steps.resolve-token.outputs.configured == 'true'
        id: discover
        env:
          GH_TOKEN: ${{ steps.resolve-token.outputs.token }}
        run: |
          URL=$(gh api graphql -f owner="${{ github.repository_owner }}" -f name="${{ github.event.repository.name }}" -f query='
            query($owner:String!, $name:String!) {
              repository(owner:$owner, name:$name) {
                projectsV2(first: 10) { nodes { title url } }
              }
            }' -q '.data.repository.projectsV2.nodes[] | select(.title | test(" — Nimbus Code Roadmap$")) | .url' 2>/dev/null | head -1 || true)
          if [ -z "$URL" ]; then
            echo "::warning::Board deste repositório ainda não existe (nome esperado: '<repo> — Nimbus Code Roadmap'). Rode 'ensure-github-project.yml' ou 'scripts/setup-github-project.sh' primeiro. Este item não foi adicionado a nenhuma board."
            echo "found=false" >> "$GITHUB_OUTPUT"
          else
            echo "found=true" >> "$GITHUB_OUTPUT"
            echo "url=$URL" >> "$GITHUB_OUTPUT"
          fi

      - name: Adicionar item à board
        if: steps.resolve-token.outputs.configured == 'true' && steps.discover.outputs.found == 'true'
        uses: actions/add-to-project@v2.0.0
        with:
          project-url: ${{ steps.discover.outputs.url }}
          github-token: ${{ steps.resolve-token.outputs.token }}
