# Security Hardening

- store secrets in a vault, never inline;
- propagate correlation IDs in every audit event;
- require SSO context for discovery and validation;
- keep advisory Terraform validation non-blocking until policy upgrade.
